{
  "name": "AntColony",
  "version": "0.1.0",
  "description": "A ant colony simulation for our website",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",    
    "build-prod": "browserify index.js  -o app.js -d -v",
    "watch-prod": "watchify index.js  -o app.js -d -v",
    "build-dev": "browserify start.js -o app.js -d -v",
    "watch-dev": "watchify start.js -o app.js -d -v"
  },
  "author": "Alexandre Vallette",
  "license": "MIT",
  "dependencies": {
    "delaunay-triangulate": "^1.1.6",
    "parse-svg-path": "^0.1.1"
  }
}
